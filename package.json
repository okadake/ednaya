{
  "name": "ednaya",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "fetch-notion": "node scripts/fetch-notion-gallery.js",
    "build:with-notion": "npm run fetch-notion && npm run build",
    "deploy": "node deploy.js",
    "deploy:full": "node deploy-full.js",
    "build-deploy": "npm run build && npm run deploy",
    "build-deploy:full": "npm run build && npm run deploy:full"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.2.1",
    "glightbox": "^3.3.1",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "ftp-deploy": "^2.4.7"
  }
}
