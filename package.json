{
  "name": "ednaya",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && mkdir -p dist/data && cp src/data/keizo-gallery.json dist/data/",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "node deploy.js",
    "build-deploy": "rm -rf dist && npm run build && npm run deploy",
    "add-gallery-images": "node scripts/add-gallery-images.js && npm run build-deploy"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^6.2.1",
    "glightbox": "^3.3.1",
    "tailwindcss": "^4.2.4"
  },
  "devDependencies": {
    "dotenv": "^17.4.2",
    "ftp-deploy": "^2.4.7"
  }
}
